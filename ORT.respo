export function ORT_RESPO(ort){
    return {
        antwort: `ORTE‑Antwort für ${ort.frame}`,
        drift: ort.drift || "D5‑Drift",
        stabil: ort.stabil || "12e‑Stabil",
        lage: ort.lage || "6D‑Lage",
        tmp: ort.tmp || "TMP‑Konstellation",
        frame: "ORTE‑RESPO"
    };
}
